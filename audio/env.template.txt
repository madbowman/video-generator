# CHATTERBOX TTS ENVIRONMENT CONFIGURATION
# Copy this to .env and customize

# ===== CPU OPTIMIZATION SETTINGS =====
# Set to your CPU thread count (your system has 24 threads)
CPU_THREADS=24
OMP_NUM_THREADS=24
MKL_NUM_THREADS=24
OPENBLAS_NUM_THREADS=24
VECLIB_MAXIMUM_THREADS=24
NUMEXPR_NUM_THREADS=24

# Advanced CPU settings (usually don't need to change)
MKL_DYNAMIC=FALSE
KMP_AFFINITY=granularity=fine,compact,1,0
KMP_BLOCKTIME=1

# ===== CHATTERBOX SETTINGS =====
# Device to use: "cpu" or "cuda" (when RTX 5070 is supported)
DEVICE=cpu

# Default voice settings
DEFAULT_CFG_WEIGHT=0.4
DEFAULT_EXAGGERATION=0.5
DEFAULT_PAUSE_MS=600
DEFAULT_TEMPERATURE=0.7

# ===== PATHS =====
# Where your voice samples are stored
VOICE_DIR=voices

# Where output audio files go
OUTPUT_DIR=outputs

# Default narrator voice (leave empty to use Chatterbox default)
DEFAULT_NARRATOR_VOICE=

# ===== PROCESSING SETTINGS =====
# Show progress bars during generation
SHOW_PROGRESS=true

# Batch processing settings
BATCH_PROCESS=false
BATCH_INPUT_DIR=stories
BATCH_OUTPUT_DIR=outputs

# ===== PERFORMANCE =====
# Enable torch inference mode (faster, less memory)
TORCH_INFERENCE_MODE=true

# Memory optimization
TORCH_CUDNN_BENCHMARK=false
TORCH_BACKENDS_CUDNN_DETERMINISTIC=true
